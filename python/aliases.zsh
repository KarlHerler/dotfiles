alias py="python"                                                                    
alias pyvenv="source ./venv/bin/activate"
alias serve="python -m SimpleHTTPServer"    # not really a python shortcut but it serves the current directory as a webserver 
